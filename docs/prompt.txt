[ ] la riparten
[ ] i18n non cambia le lingue ne in lvgl ne in web
[ ] le lingue in /config vanno trattate cosi : 
lingua Pannello: definisce la lingua di defaul del pannello touch (modificabile dal cliente) : va persistita ma non modificata subito nel display
lingua backend: definisce la lingua da usarfe per l'interfaccia web : va persistita e cambiata subito rinfrescando invalidanto la lingua e le pagine
[ ] Tasto STOP si esecuzione programma : alla prima pressione devi mostrare il messaggio di conferma : se premuto devi proseguire con il programma in corso ma alla fine devi disabilitare la ripetizione automatica del programma a fine ciclo anche con credito disponibile. 
[ ] Allinea la grafica di Emulator con quella lvgl, tenedo invariata la parte a dx del pannello
[ ] In /test -> I/O expander lo stato degli inmput non viene rilevato per DIP1 2 3
[ ]Controlla lo logica della FSM : 
- all'avvio deve inizializzare tutto l'hardware e sincronizzarsi con il server remoto e NTP : va introdotto un nuovo stato FSM_STATE_INIT
- poi deve passare in attesa di presenza cliente (tramite tocco del touch) mostrando lo slideshow nel riquadro centrale della pagina con il tasto con il tasto Selezione Lavaggio e il messaggio di errore opzionale in basso : va introdotto un nuovo stato FSM_STATE_SLIDESHOW
- alla pressione di selezione lavaggio deve andare alla pagina programmi
[ ] Va previsto che si siano più ioExpanders Modbus: l'interfaccia deve adattarsi a più schede 
[ ] Attuale funzionamento: sono in ads -> inserisco una moneta -> ritorna in ads ma non accetta il button Seleziona lavaggio
[ ] va aggiunto un parametro in /config -> Timeouts con la percentuale di ciclo eseguito per passare alla segnalazione di PreFineCiclo: un numero intero 0-99 che esprime la peercentuale del tempo rimanente per la fine  
[ ] nell'esecuzione del programma al raggiungimento 
[ ] i pulsanti programma devono cambiare colore al raggiungimento del 70%
[ ] il caricamento delle immagini per lo slideshow segue questo ordine: se ci sono file imgxx.jpg in SPIFFS vanno caricate da SPIFFS, se non ci sono in SPIFFS le cerchiamo in SDCARD. se non ci sono in SDCARD saltiamo la pagina pubblicità e passiamo direttamente a scelta programmi
[ ] Font da utilizzare : pagina ads : testo nome impianto GoogleSans70, Selezione lavaggio GoogleSans50, messaggio errore GoogleSans40

[ ] Far eseguire le funzioni tra moduli diversi (ad esempio un comando cctalk lanciato da una funzione lvgl) tramite la coda messaggi e non con chiamate dirette. Verificare il codice attuale se sono implementate funzioni che fanno chiamate a scope diversi e creare un'elenco con la descrizione di ogni operazione trans-scope
[ ] al termine della schermata di init con il logo va effettuato l'invio della sequenza CCTALK cctalk_driver_start_acceptor()
[ ]-definire la maschera di attivazione dei relays nelle righe dei programmi in /config/programs mediante più gruppi di editbox da 4 caratteri  ciascuno (accettano 0 o 1 - 4 caratteri obbligatori fillati con trailing-zeroes) in base al numero di porte di out disponibili. a sx degli editbox ci sarà l'indicazione del numero relauys (1-4, 5-12, ecc.)  1 relays 1-4 sono quelli sulla MH1001,  i successi gruppi di 8 sulle schede waveshare RS485 in modbus. Formattare gli editbox su più righe nella stessa DIV (1 riga per device)
[ ] Utilizzare i testi delle chiavi program_name_* peer la compilazione dei nomi dei programmi, ed aggiungere in /programs la scelta della lingua 
[ ] Spostare la voce Abilita log POST da /test alla sezione Remote Logging di /config
[ ] invece di utilizzare  bsp_display_brightness_set crea una funzione in device_config per effettuare la chiamata i2c sul bus i2c in uso al display
[ ] il pulsante programma selezionato deve apparire con un bordo bianco 5px, se viene messo in pausa lo sfondo deve diventare blu
[ ] Gestione di digital_io:
1. Input : la lettura di un cabio stato 0->1 su un input digitale deve portare an un controllo se l'input sia associato ad un tasto programma del touch e se si deve generare la stessa azione. Se non è associato deve chiamare una funzione de elaborazione degli altri input (per ora senza implementare nulla)
2. output : all'avviamento di un programma vanno attivati gli output negli stati definiti in tabella programmi fino al termine del programma o fino alla sua interruzione definitiva (STOP)
3. gli eventi digital_io vanno reinseriti nella mailbox e trattati come descritoo in questa nota
[ ]  dobbiamo rivedere digital_io : la scheda una gli expander interni i2c in questo modo: 
innanzitutto creiamo un task 'io_process' che sarà il consumer di tutti gli eventi digital_io non già consumati da altri moduli
OUT : WHITE_LED, BLUE_LED,RELAY3,RELAY4,RELAY2,RELAY1,RED_LED,HEATHER1
IN  : DIP1, DIP2, DIP3 , SERVICE_SWITCH, OPTO2, OPTO1, OPTO4, OPTO3
Dobbiamo mappare tutti gli input nel device considerando gli input OPTOx come possibili tasti e gli altri come segnali da far processare al task io_process (per ora non agisce) , gli out considerando RELAYx come relay da pilotare e da usare nella lista programmi e i LED come segnali da utilizzare per eventi usando come consumer task il io_process (per ora non agisce), lo stato di HEATER va processato dal task io_process (per ora non agisce)

-----------------
le due lingue sono completamente separate e gestite con logiche diverse:
LVGL : cambio su richiesta dell'utente e stringhe da caricare in PSRAM all'avvio
WEB : Scelta effettuata dal manutentore con rilettura del file i18n ed adeguamento dell'interfaccia web
Non c'è relazione tra le 2 lingue e non posso settarle uguali
Dobbiamo verificare le operazioni al boot per :
Caricamento lingue per LVGL in PSRAM ed attivazione della lingua di default definita in config
Caricamento di 1 sola lingua per backend in base al config
Cambiamento della lingua LVGL in base alla scelta utente usando la tabella in PSRAM
Cambiamento della lingua di backend in base alla richiesta utente web (manutentore) e rilettura del json con successiva invalidazione pagine .
Per ora prepara un report della situazione e il programma per la correzzione che salverai in 260319_LANGUAGE_ISSUES.md
------------------

------------------
!IMPORTANT
ca completata l'unificazione della gestione di config che ha parti in wen_ui.c
------------------

------------------
I (19234) APP: [F] [M] Inizio sequenza di inizializzazione CCTalk...
I (19254) APP: [F] [M] Cmd1 - Address Poll: OK
I (19334) APP: [F] [M] Cmd2 - Modify Inhibit Status (all channels): OK
I (19404) APP: [F] [M] Cmd3 - Modify Master Inhibit (accept enabled): OK
I (19474) APP: [F] [M] Cmd4 - Request Inhibit Status: OK (mask=0xFFFF)
I (19474) APP: [F] [M] Sequenza CCTalk completata


[ ] gestione brightness:
L'utilizzo dell'**ESP32-P4** per controllare questo monitor tramite MIPI-DSI e I2C è un'ottima scelta, data la potenza del chip nel gestire flussi video. Per la luminosità, Waveshare espone un'interfaccia I2C specifica che funge da "ponte" verso il driver della retroilluminazione.

Ecco i dettagli tecnici per implementare il controllo sul tuo ESP32-P4.

---

❯ in precedenza usavamo per la gestione delle pagine html e i relativi javascript il sistema che prevedeva che nel file wepages_embedded.c ci fossero i sorgenti html e js delle pagine stesse, che poi venivano estratti da uno script python. Poi abbiamo cambiato con una gestioen diretta di html e js ma il file
  webpages_embedde.c è rimansto. Fai un report sull'attuale impiego di questo file e verifica se possiamo toglierlo dal codice e dalla documentazione  


