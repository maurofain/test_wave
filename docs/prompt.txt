[ ] Correggere i JS e/o gli html di /config per visualizzare tutte le sezioni abilitate (ad esempio i/o expander)
[ ] ampliamo la sezione /test/IOexpander per poter controllare anche gli I/O su scheda waveshare via RS485: Raggruppa i pulsanti IN e Uot in 2 file da 4 (non 6+2) , poi metti un altra sezione con nome Wavwshare con gli stessi pulsanti della sezione Chip043 ma numerate da IN8-IBN15 e OUT8-OUT15. Collega questi nuovi tasti a delle funzioni che controllino la scheda Waveshare secondo le specifiche in Modbus.md. 
All'avvio della app dopo l'inizializzazione di CCTALK inizializza la RS485 e la scheda waveshare. 
[ ] Dopo l'inserimento della moneta ritorna alla schermata ads e poi lingue
[ ]Controlla lo logica della FSM : 
- all'avvio deve inizializzare tutto l'hardware e sincronizzarsi con il server remoto e NTP : va introdotto un nuovo stato FSM_STATE_INIT
- poi deve passare in attesa di presenza cliente (tramite tocco del touch) mostrando lo slideshow nel riquadro centrale della pagina con il tasto con il tasto Selezione Lavaggio e il messaggio di errore opzionale in basso : va introdotto un nuovo stato FSM_STATE_SLIDESHOW
- alla pressione di selezione lavaggio deve andare alla pagina programmi
[ ] La gestione della striscia LED deve prevedere che ci sono 2 strisce separate di lunghezza uguale ma che agiscono sincrone (stesso colore led per posizione), con una effetto da definire per la situazione di IDLE, durante il run segue aumenta a partire dal primo led ad accedndere sezioni di verde, al raggiugimento di PreFineCiclo commuta in porpora e a ciclo finito rimane completamente accesa lampeggioante a 2hz in colore porpora fino a che la macchina ritorna in IDLE o viene premuto qualsiasi funzione sui tasti o touch che la portano a spegnersi
[ ] va aggiunto un parametro in /config -> Timeouts con la percentuale di ciclo eseguito per passare alla segnalazione di PreFineCiclo: un numero intero 0-99 che esprime la peercentuale del tempo rimanente per la fine  
[V] la barra di progressione deve apparire solo dopo che è stato scelto il programma: deve partire dal 10% (con tutto il tempo da utilizzare) crescere al 100% quando il tempo è finito. Quando il residuo è => a PreFineCiclo% i tutti i pulsanti , il bordo dell'area crediti, la barra di progressione e la barra led diventanono porpora 
[ ] nell'esecuzione del programma al raggiungimento 
[*] Il tasto Stop deve apparire solo a programma avviato, il tasto programma selezionato deve cambiare il titolo in "Sospendi" se premuto durante l'esecuzione e alla pressione successiva (riavvio da sospensione) rimettere il nome del programma.
[ ] i pulsanti programma devono cambiare colore al raggiungimento del 70%
[ ] il caricamento delle immagini per lo slideshow segue questo ordine: se ci sono file imgxx.jpg in SPIFFS vanno caricate da SPIFFS, se non ci sono in SPIFFS le cerchiamo in SDCARD. se non ci sono in SDCARD saltiamo la pagina pubblicità e passiamo direttamente a scelta programmi
[ ] Font da utilizzare : pagina ads : testo nome impianto GoogleSans70, Selezione lavaggio GoogleSans50, messaggio errore GoogleSans40

[ ] Far eseguire le funzioni tra moduli diversi (ad esempio un comando cctalk lanciato da una funzione lvgl) tramite la coda messaggi e non con chiamate dirette. Verificare il codice attuale se sono implementate funzioni che fanno chiamate a scope diversi e creare un'elenco con la descrizione di ogni operazione trans-scope
[ ] al termine della schermata di init con il logo va effettuato l'invio della sequenza CCTALK cctalk_driver_start_acceptor()
[ ]-definire la maschera di attivazione dei relays nelle righe dei programmi in /config/programs mediante più gruppi di editbox da 4 caratteri  ciascuno (accettano 0 o 1 - 4 caratteri obbligatori fillati con trailing-zeroes) in base al numero di porte di out disponibili. a sx degli editbox ci sarà l'indicazione del numero relauys (1-4, 5-12, ecc.)  1 relays 1-4 sono quelli sulla MH1001,  i successi gruppi di 8 sulle schede waveshare RS485 in modbus. Formattare gli editbox su più righe nella stessa DIV (1 riga per device)
[ ] Utilizzare i testi delle chiavi program_name_* peer la compilazione dei nomi dei programmi, ed aggiungere in /programs la scelta della lingua 
[ ] Spostare la voce Abilita log POST da /test alla sezione Remote Logging di /config
-----------------
## refactor i18n: ristrutturiamo tutta la localizzazione
- La lingua di riferimento è l'italiano
- separiamo la parte web dalla parte lvgl
- lvgl: 
    - abbiamo poche stringhe da gestire: utilizzaimo un file json con un array della lingue, in ogni elemento ci sarà una array composto da tuple chiave:valore con i testi da visualizzare. 
    - all'avvio carichiamo tutte le lingue in PSRAM
    - nelle pagine lvgl al posto delle stringhe effettive mettiamo dei placeholder {{KEY}} che vengono espansi nel valore della lingua corrente
- html
    - qui ci sono più stringhe da gestire e quindi modifichiamo la struttura : utilizzaimo un file json con un array della lingue, in ogni elemento ci sarà un array di pagine web che contiene una array composto da tuple chiave:valore con i testi da visualizzare. La chiave è un valore numerico univoco per pagina 
    - all'avvio carichiamo la lingua corrente in PSRAM : 
        - si creano dei blob (1 per ogni pagina) in cui le stringhe sono memorizzate una di seguito all'altra in ordine numerico di KEY separate da un carattere 0x00.
        - Eventuali interruzzioni nella continuità della numerazione comporteranno l'inserimento di 0x00 per ogni numero mancante (es. abbiamo key 25 e poi 28: andranno inserti 2 0x00). 
        - durante la compilazione si salva la posizione di inizio di ogni stringa (anche equelle vuote o di riempimento sequenza) in un array di uint_16t . 
        - le pagine web conterranno al posto delle stringhe localizzate dei placeholder {{KEY}} che verranno sostiuiti con le stringhe i18n prima della pubblicazione al browser.
    . in questo modo la il preporocessore delle pagine web trova i placeholder, ricava dall'array posizioni la posizione della stringa da pubblicare (termonata da 0) la sostiusce e ripete per ogni placeholder.
    in fase di sviluppo ogni nuovo inserimento di testo deve verificare se la stringa esiste già ed utilizzare la KEY gia assegnata, oppure creare un nuovo record nel set della pagina web in creazione con la prossima KEY e la nuova stringa 
------------------
## refactor i18n v2 (decisioni aggiornate)
- Pipeline web primaria: HTML-first su SPIFFS (`data/www/*.html` + `data/www/js/*.js`), senza passaggio di conversione embedded->html nel flusso principale.
- Localizzazione web: preload al boot (o su cambio lingua) con cache pagine localizzate in PSRAM; nessun parsing JSON durante la singola richiesta HTTP.
- Placeholder web: numerici compatti a 3 cifre (`{{001}}..{{999}}`) con validazione formale.
- Catalogo JSON i18n: per ogni chiave web includere anche `label` alfanumerica descrittiva per editing/manutenzione.
- Runtime contract: il parser usa solo la chiave numerica; `label` non è usata a runtime.
- Fallback obbligatorio: lingua richiesta -> `it` -> stringa vuota.
- JS: separazione script inline in moduli dedicati; include ordinati (`core -> page -> init`).
- `i18n_editor` v2:
  - supporto schema nuovo (key numerica + `label`)
  - aggiunta nuove chiavi da UI (assegnazione automatica prossima libera per pagina)
  - validazioni bloccanti su duplicati, formato `label`, presenza `text.it`.
- Validatore build-time:
  - errori su placeholder invalidi/non mappati, key duplicate, `label` invalida, `text.it` mancante
  - warning su traduzioni non-base mancanti e chiavi non usate.
------------------
!IMPORTANT
ca completata l'unificazione della gestione di config che ha parti in wen_ui.c
------------------
[X]Nella cartella i18n_editor Crea webuna app python da far girare su un pc windows o linux che permetta di editare i file delle lingue:
- si sceglie lo scope ed appaiono tutte le stringhe di quello scope incolonnate per lingua, la prima colonna è l'ID. la prima lingua è ITALIANO, Ogni voce è editabile e di fianco alla textbox ha un button per la traduzione automatica da fare interrogando un servizio web definito in un file di configurazione. In alto a sx abbiamo la scelta dello scope e a destra i tasti SALVA e ESCI. Il salvataggio crea automaticamente un json con tutti i dati di tutte le lingue con nome 'i18n_{tiomestamp}.json'
------------------
I (19234) APP: [F] [M] Inizio sequenza di inizializzazione CCTalk...
I (19254) APP: [F] [M] Cmd1 - Address Poll: OK
I (19334) APP: [F] [M] Cmd2 - Modify Inhibit Status (all channels): OK
I (19404) APP: [F] [M] Cmd3 - Modify Master Inhibit (accept enabled): OK
I (19474) APP: [F] [M] Cmd4 - Request Inhibit Status: OK (mask=0xFFFF)
I (19474) APP: [F] [M] Sequenza CCTalk completata


[ ] gestione brightness:
L'utilizzo dell'**ESP32-P4** per controllare questo monitor tramite MIPI-DSI e I2C è un'ottima scelta, data la potenza del chip nel gestire flussi video. Per la luminosità, Waveshare espone un'interfaccia I2C specifica che funge da "ponte" verso il driver della retroilluminazione.

Ecco i dettagli tecnici per implementare il controllo sul tuo ESP32-P4.

---

### Specifiche del Protocollo I2C

Sulla scheda del display, il controllo della luminosità è gestito da un microcontrollore dedicato che risponde a un indirizzo specifico.

* **Indirizzo I2C del Dispositivo:** `0x45`
* **Indirizzo del Registro:** `0x86`
* **Gamma Valori:** `0` (0% - Spento) a `255` (100% - Luminosità Massima).

---

### Implementazione su ESP32-P4 (ESP-IDF)

Poiché l'ESP32-P4 utilizza le versioni più recenti dell'ESP-IDF (v5.3+), è consigliabile usare il nuovo driver `i2c_master`.

#### 1. Configurazione del Bus

Assicurati di collegare i pin SDA/SCL del monitor ai pin GPIO dell'ESP32-P4 che hai definito (es. GPIO 7 e 8 sono comuni per I2C su questo chip).

```c
#include "driver/i2c_master.h"

i2c_master_bus_handle_t bus_handle;
i2c_master_bus_config_t bus_config = {
    .clk_source = I2C_CLK_SRC_DEFAULT,
    .i2c_port = I2C_NUM_0,
    .scl_io_num = GPIO_NUM_8,
    .sda_io_num = GPIO_NUM_7,
    .glitch_ignore_cnt = 7,
};
ESP_ERROR_CHECK(i2c_new_master_bus(&bus_config, &bus_handle));

i2c_master_dev_handle_t dev_handle;
i2c_device_config_t dev_config = {
    .dev_addr = 0x45,
    .device_config.device_address = 0x45,
    .scl_speed_hz = 100000, // 100kHz standard
};
ESP_ERROR_CHECK(i2c_master_bus_add_device(bus_handle, &dev_config, &dev_handle));

```

#### 2. Funzione di regolazione

Per cambiare la luminosità, invia il registro seguito dal valore desiderato:

```c
esp_err_t set_backlight_brightness(uint8_t level) {
    uint8_t write_buf[2] = {0x86, level};
    return i2c_master_transmit(dev_handle, write_buf, sizeof(write_buf), -1);
}

```

---

### Note Importanti per ESP32-P4

* **Alimentazione:** Il monitor richiede 5V stabili per la retroilluminazione. Assicurati che l'ESP32-P4 e il monitor condividano la massa (GND).
* **Conflitto Indirizzi:** Se hai collegato anche il touch, l'indirizzo del touch controller (Goodix) sarà solitamente `0x5D` o `0x14`. Non confonderlo con lo `0x45` del backlight.
* **Inizializzazione DSI:** Ricorda che la retroilluminazione potrebbe non accendersi se il segnale MIPI-DSI non è attivo o se il pannello non è stato correttamente inizializzato tramite i comandi DSI (solitamente gestiti dai componenti BSP di Espressif come `esp_lcd_ek79007`).

---

[Display Waveshare DSI Setup](https://www.youtube.com/watch?v=liIlQvlp9Co)
Questo video mostra l'interfaccia e i collegamenti fisici necessari per far funzionare correttamente i display Waveshare DSI, inclusa la gestione della luminosità.

Vuoi che ti aiuti a integrare questo codice con l'inizializzazione del driver video MIPI-DSI specifico per l'ESP32-P4?

