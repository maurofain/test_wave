# Report attività — 26/03/2020

## Obiettivo giornata
Allineare gestione crediti/programmi tra UI, FSM e keepalive, correggendo anomalie su addebiti, visualizzazione credito e saturazione coda eventi.

## Operazioni eseguite

### 1) Auto-restart programmi e allineamento timeout
- Riattivata la pubblicazione evento reale per auto-restart programma in pagina LVGL programmi.
- Rimossa la logica di timeout/reset credito lato UI (duplicata/parziale) per centralizzare la regola in FSM.
- Aggiornata FSM per timeout in `CREDIT` con distinzione sessioni:
  - virtuale (`QR/CARD`) -> chiusura sessione + azzeramento credito;
  - moneta (`COIN`) -> trattenuta/azzeramento credito a timeout.
- Ripristinata la bandierina lingua nella pagina programmi tramite chrome comune.

### 2) Chiarimento modello **Crediti vs Euro**
- Definita e implementata la regola: **1 credito = 1 euro**.
- Gli ingressi pagamento restano in centesimi, ma ora la conversione in crediti avviene in modo centralizzato nel FSM.
- Introdotti residui separati per centesimi non convertiti:
  - `ecd_cents_residual`
  - `vcd_cents_residual`
- L’avvio/rinnovo programma scala solo i **crediti** (`price_units`), non i centesimi.
- Aggiornati log FSM su aggiunta credito e consumo credito.

### 3) Keepalive: visibilità completa credito
- Esteso il log keepalive con snapshot FSM:
  - `ecd`, `vcd`, `credit`
  - `ecd_res`, `vcd_res`
- Mantenuti i log su input/output digitali e telemetria temperatura/umidità.

### 4) UI area credito
- Modificata area `Crediti` per mostrare **sempre il credito**.
- Rimossa visualizzazione del tempo dall’area credito.
- Forzata etichetta sempre su `Crediti` (la scritta `Secondi` non appare più).

### 5) Fix saturazione coda mailbox FSM
- Analizzato flood log `ENQUEUE FULL` con `action=43`.
- Identificato `action=43` = `ACTION_ID_DIGITAL_IO_GET_OUTPUT` (richieste da web/polling).
- Corretto instradamento in `tasks`:
  - operazioni `GET_OUTPUT/GET_INPUT/GET_SNAPSHOT` eseguite direttamente su `digital_io`;
  - solo `SET_OUTPUT` resta sul percorso agente/mailbox.
- Risultato: eliminato il percorso che saturava la coda FSM con richieste di sola lettura.

## File modificati
- `components/lvgl_panel/lvgl_page_programmi.c`
- `main/fsm.c`
- `main/fsm.h`
- `components/http_services/keepalive_task.c`
- `main/tasks.c`

## Verifiche eseguite
- Build ripetuta più volte con esito positivo:
  - `get_idf && idfc -b` 

## Aggiornamento operativo (fine sessione)
- Verificata nuovamente la compilazione con esito positivo:
  - `get_idf && idfc -b` 
- Definita regola operativa per le prossime attività della giornata:
  - aggiornare `docs/260320 LOG.md` dopo ogni operazione rilevante eseguita.

## Registro prompt e azioni (cronologico)
- **Prompt utente:** `regola : tieni aggiornato il file di logo dopo ogni operazione`
  - **Azione eseguita:** recepita la regola operativa e impostato l'aggiornamento del log dopo ogni attività rilevante.
- **Prompt utente:** `il file di log delle tue operazioni , oggi à 260320 LOG.md`
  - **Azione eseguita:** confermato il file giornaliero `docs/260320 LOG.md` e aggiornato con la regola operativa.
- **Prompt utente:** `ogni giorno avrà il suo log. Nel log aggiungi anche i miei propt seguiti dalle tue azioni`
  - **Azione eseguita:** adottato formato giornaliero con sezione `Registro prompt e azioni` da mantenere aggiornata.
- **Prompt utente:** `verifica se all'esecuzione del programma vengono settati relays e resettati alla fine`
  - **Azione eseguita:** verificato flusso in `main/tasks.c` e `components/web_ui/web_ui_programs.c`; confermato set relay a start programma e reset relay al rientro in `CREDIT`.
- **Prompt utente:** `i relays non si azionano: ho controllato in modalità test e funziono sia quelli su MH1001 che quelli modbus. Ricontrolla digital_io se indirizza correttamente i relays`
  - **Azione eseguita:** individuata e corretta anomalia di indirizzamento relay nel mapping programma (`relay_number` usato come `output_id` diretto). Aggiornato `digital_io_program_relay_to_output_id` in `components/digital_io/digital_io.c` (R1..R4 -> output locali relay dedicati, R5..R12 -> OUT09..OUT16 Modbus) e allineato uso mapping in `components/web_ui/web_ui_programs.c` per set/get relay. Eseguita build `get_idf && idfc -b` con esito positivo.
- **Prompt utente:** `metti il messaggio 'Tabella programmi salvata' in /config/programs su popup invece che in cima alla scheda`
  - **Azione eseguita:** modificato `data/www/js/programs_04.js` in `savePrograms()` sostituendo il feedback inline (`showStatus`) con popup browser (`window.alert`) per il solo caso di salvataggio riuscito.
- **Prompt utente:** `in sezione chrome mostra anche i secondi nell'orologio a sx`
  - **Azione eseguita:** aggiornato `components/lvgl_panel/lvgl_page_chrome.c` in `chrome_update_time_label()` passando il formato da `%H:%M` a `%H:%M:%S` e placeholder da `--:--` a `--:--:--`.
- **Prompt utente:** `dammi una descrizione completa del mapping di digital_io`
  - **Azione eseguita:** analizzato mapping completo in `components/digital_io/include/digital_io.h`, `components/digital_io/digital_io.c` e configurazione default Modbus in `components/device_config/device_config.c` per documentare canali locali/Modbus, formula indirizzi e mapping relay logici programma.
- **Prompt utente:** `completa la tabella IO_MAP.md`
  - **Azione eseguita:** completato `docs/IO_MAP.md` con struttura aggiornata e tabelle complete per uscite (R1..R12, LED, HEATER), ingressi locali (I1..I8) e ingressi Modbus (I9..I16), includendo note su `INxx/OUTxx` e offset `relay_start/input_start`.
- **Prompt utente:** `abbiamo 10 programmi e 12 relays, rivediamo l'associazione in /config/programs. mettiamo per ogni riga programma un checkbox con il numero/device del relay in hover, e nella seconda riga mettiamo 4 checkbox per gli out diversi (led e heater)`
  - **Azione eseguita:** modificati `data/www/js/programs_04.js` e `data/www/programs.html`: sostituito editor bitmask con checkbox a due righe per ogni programma (riga 1: R1..R12, riga 2: LED1/LED2/LED3/HEATER), aggiunto tooltip hover `numero/device`, mantenendo la persistenza su `relay_mask`.
- **Prompt utente:** `perche avviene e come avitiamo questo warning : W (...) keepalive_task: [C] Snapshot digital_io non disponibile per keepalive: ERROR`
  - **Azione eseguita:** analizzata catena `keepalive_send_message()` -> `digital_io_get_snapshot()`: il warning scatta quando almeno un canale (locale o Modbus) fallisce in lettura e `first_err` viene propagato. Identificate cause tipiche (IO expander disabilitato/non pronto, RS485/Modbus non disponibili, timeout) e proposte mitigazioni (declassare warning per errori non critici e/o rendere snapshot keepalive tollerante con fallback parziale).
- **Prompt utente:** `togli le label R1R.. e LED...`
  - **Azione eseguita:** aggiornati `data/www/js/programs_04.js` e `data/www/programs.html` rimuovendo il testo visibile delle label checkbox nella colonna associazioni programmi (`R*` e `LED/HEATER`), mantenendo i tooltip hover `numero/device`.
- **Prompt utente:** `perchè è apparso questo errore: mb descriptor table size is incorrect ...`
  - **Azione eseguita:** analizzato path init Modbus in `components/modbus_relay/modbus_relay.c`; rilevato che viene chiamato `mbc_master_start()` senza una precedente registrazione della descriptor table (`mbc_master_set_descriptor` non presente nel progetto), causa compatibile con `ESP_ERR_INVALID_ARG` su start.
- **Prompt utente:** `ora il log si è riempito di queste righe ... Modbus TX/RX ...`
  - **Azione eseguita:** applicata mitigazione anti-flood su due livelli: in `main/tasks.c` il `digital_io_task` ora legge solo ingressi locali (`IN01..IN08`) invece di `digital_io_get_snapshot()` completo (che innescava polling Modbus continuo), e in `components/modbus_relay/modbus_relay.c` i log TX/RX ciclici sono stati declassati da `ESP_LOGI` a `ESP_LOGD`.
- **Prompt utente:** `aggingiamoin /config una sezione Modbus con abilitazione e editbox per il numero di moduli Waveshare installati`
  - **Azione eseguita:** aggiunto script `data/www/js/config_modbus_ui.js` e relativo include in `data/www/config.html` per creare in `/config` una sezione Modbus con checkbox di abilitazione e input numero moduli Waveshare; integrato salvataggio su `/api/config/save` mappando il numero moduli in `modbus.relay_count` e `modbus.input_count`.
- **Prompt utente:** `fai un build`
  - **Azione eseguita:** eseguita build di verifica con `get_idf && idfc -b`; build fallita su `components/lvgl_panel/lvgl_page_programmi.c:454` per chiamata errata a `lv_obj_set_style_border_color` (argomento mancante).
- **Prompt utente:** `correggi`
  - **Azione eseguita:** corretta la chiamata in `components/lvgl_panel/lvgl_page_programmi.c` da `lv_obj_set_style_border_color(btn, COL_GREY LV_PART_MAIN)` a `lv_obj_set_style_border_color(btn, COL_GREY, LV_PART_MAIN)`; rilanciata build `get_idf && idfc -b` con esito positivo.
- **Prompt utente:** `cos'è questo errore: ... mb descriptor table size is incorrect ...`
  - **Azione eseguita:** analizzato errore runtime Modbus e identificata la causa nell'inizializzazione del master senza descriptor table valida registrata prima di `mbc_master_start()`.
- **Prompt utente:** `correggi`
  - **Azione eseguita:** aggiornato `components/modbus_relay/modbus_relay.c` aggiungendo descriptor table (`mb_parameter_descriptor_t`) con CIDs coerenti, chiamata `mbc_master_set_descriptor(handler, descriptors, MODBUS_RELAY_CID_COUNT)` prima di `mbc_master_start(handler)` e init esplicita di `param_opts`; build `get_idf && idfc -b` completata con esito positivo.
- **Prompt utente:** `non fai più l'aggiornamento del tuo LOG`
  - **Azione eseguita:** ripristinato aggiornamento immediato del file `docs/260320 LOG.md` dopo le operazioni rilevanti.
- **Prompt utente:** `mi pare che alla scelta programmi non siano gestiti gli out diversi dai relays`
  - **Azione eseguita:** verificato disallineamento tra frontend `/config/programs` (mask su `OUT01..OUT16`, inclusi `LED/HEATER`) e backend (`web_ui_program_apply_outputs`) che applicava ancora la mask come relay logici `1..12`. Corretto `components/web_ui/web_ui_programs.c` per applicare e azzerare direttamente gli output `1..DIGITAL_IO_OUTPUT_COUNT` tramite `tasks_digital_io_set_output_via_agent`, includendo quindi anche uscite non-relay. Build di verifica eseguita con successo: `get_idf && idfc -b`.
- **Prompt utente:** `controlla la persistenza della tabella programmi in particolare per l'abilitazione degli output`
  - **Azione eseguita:** verificato end-to-end il ciclo persistenza (`/api/programs/save` -> `web_ui_program_table_update_from_json` -> `programs_save_to_storage` su `/spiffs/programs.json` -> reload con `programs_try_load_from_storage`). Confermato che i campi `enabled` (boolean) e `relay_mask` (uint16, bit output `OUT01..OUT16`) vengono serializzati, validati e ricaricati correttamente; controllo incrociato anche sul file `data/programs.json` con mask multi-bit presenti.
- **Prompt utente:** `controlla se i bit degli output sono letti e scritti nello stesso ordine`
  - **Azione eseguita:** verificati i punti di lettura/scrittura mask in frontend (`outputIsEnabled`, `setOutputEnabled`) e backend (`web_ui_program_apply_outputs`). Confermata coerenza ordine bit: `bit 0 -> OUT01`, `bit 1 -> OUT02`, ... `bit 15 -> OUT16`; nessuna inversione rilevata tra salvataggio, rendering checkbox e applicazione uscite.
- **Prompt utente:** `questo error è evitabile? E (6139) HTTP_CLIENT: Incomplete chunked data received -1`
  - **Azione eseguita:** analizzati i call-site `esp_http_client` in `components/http_services/http_services.c`, `main/init.c` e OTA in `components/web_ui/web_ui_common.c`. Confermato che il warning è tipico di risposta chunked troncata/chiusura anticipata socket lato rete/server. Nel proxy remoto è già mitigato con accumulator eventi, header fallback (`Connection: close`, `Accept-Encoding: identity`) e tolleranza a risposta parziale 2xx; resta evitabile in modo robusto solo stabilizzando la risposta lato server (no chunk incompleti/close prematuri) o degradando/filtrando il log lato client nei flussi non critici.
- **Prompt utente:** `possiamo controlla sulla sche da modus lo stato dei relay?`
  - **Azione eseguita:** verificata disponibilità endpoint diagnostico già presente: `POST /api/test/modbus/poll` in `components/web_ui/web_ui_test_api.c`, che legge coil Modbus con `modbus_relay_read_coils(...)` e restituisce array JSON `relays[]` (stato dei relay su scheda Modbus) oltre a `inputs[]` ed eventuali errori `err_co/err_di`.
- **Prompt utente:** `quando selezioniamo un programma ed attiviamo i relay , dopo 100ms controlloliamo se i relay sono nello stato desiderato. Se fallisce riproviamo per altre 2 volte poi segnaliamo errore in s_stop_btn`
  - **Azione eseguita:** implementata logica di verifica output post-selezione programma in `main/tasks.c`: dopo applicazione uscite attesa 100ms, verifica stato reale output, retry fino a 3 tentativi totali (iniziale + 2 retry). In caso di fallimento finale viene attivato flag globale `tasks_program_outputs_verify_error_active()`. Integrata UI in `components/lvgl_panel/lvgl_page_programmi.c` per mostrare errore nel pulsante STOP (`s_stop_btn`) con testo i18n `program_outputs_error` e colore di allarme. Aggiunta chiave i18n in `data/i18n_v2.json`. Build verifica completata con successo: `get_idf && idfc -b`.
- **Prompt utente:** `quando abbiamo ricevurto un credito VCD e non dobbiamo accettare più le monete dobbiamo disabilitare la gettoniera. Terminata l'operazione che usa i VCD e resettato il pannello dobbiamo riattivare la gettoniera`
  - **Azione eseguita:** implementata orchestrazione via event bus FSM in `main/tasks.c`: durante sessione VCD lockata (`FSM_SESSION_MODE_VIRTUAL_LOCKED` con sorgente `QR/CARD` e pagamenti aggiuntivi non consentiti) viene pubblicato `ACTION_ID_CCTALK_STOP` verso `AGN_ID_CCTALK` per inibire la gettoniera; a fine sessione VCD, quando lo stato rientra a `CREDIT/ADS` (pannello resettato), viene pubblicato `ACTION_ID_CCTALK_START` per riabilitazione gettoniera. Build di verifica eseguita con successo: `get_idf && idfc -b`.
- **Prompt utente:** `attiviamo la gestione dei led, per ora non implementiamo la doppia striscia ma implementiamo la progressione e il cambio colori. In /config è definito il numerop dei led. Il comportamento è : in stato di IDLE tutti i led sono blu scuro in esecuzione programma i led aumentano in base alla % di progressione, quando siamo in Prefinecilo diventano porpora`
  - **Azione eseguita:** attivata la logica LED nel task `ws2812` in `main/tasks.c` usando `led_init()`/`led_get_count()` (conteggio da `/config`) e snapshot FSM runtime. Implementato comportamento richiesto senza doppia striscia: fuori esecuzione tutti i LED blu scuro; durante `RUNNING/PAUSED` progressione su numero LED accesi in base alla percentuale (`running_elapsed_ms/running_target_ms`); in `pre_fine_ciclo_active` cambio colore progressione a porpora. Build di verifica eseguita con successo: `get_idf && idfc -b`.
- **Prompt utente:** `nei led quando esci da IDLE devi spegnere la striscia. al termine del programma or aappare ilprimo led verde, deve essere blu. in IDLE crea un effetto per cui un led alla volta passa da 100% a 20% e poi ritorna al 100% in un ciclo di 10 secondi: è utile a far vedere che la app è attiva`
  - **Azione eseguita:** raffinata la logica `ws2812_task` in `main/tasks.c`: in uscita da `IDLE` la striscia viene azzerata con finestra di clear di 500ms; eliminata l'accensione forzata del primo LED a bassissima progressione (fix del primo LED verde); aggiunta animazione `IDLE` a ciclo 10s con un LED attivo alla volta che pulsa blu dal 100% al 20% e ritorna al 100%, mentre gli altri restano blu al 100%. Build di verifica eseguita con successo: `get_idf && idfc -b`.
- **Prompt utente:** `quando il programma finisce fai 5 lampeggi  con tutti i led in bianco a  2hz`
  - **Azione eseguita:** aggiunta in `main/tasks.c` una sequenza di fine programma nel `ws2812_task` rilevando la transizione `RUNNING/PAUSED -> CREDIT`: esegue 5 lampeggi completi (10 toggle) di tutti i LED in bianco a 2Hz (toggle ogni 250ms), poi ritorna alla visualizzazione normale (blu in stato non-IDLE). Build di verifica eseguita con successo: `get_idf && idfc -b`.
- **Prompt utente:** `se ce il riavvio automatico esegui comunque il lampeggio e poi passa alla visualizzazione programma standard`
  - **Azione eseguita:** esteso il trigger del lampeggio di fine ciclo in `main/tasks.c` da `RUNNING/PAUSED -> CREDIT` a qualunque uscita da `RUNNING/PAUSED`, coprendo anche il flusso con riavvio automatico. Al termine dei 5 lampeggi bianchi a 2Hz viene forzato il refresh dello stato LED standard corrente, così la strip torna correttamente alla visualizzazione prevista dal nuovo stato FSM.
- **Prompt utente:** `la dissolvenza dei led deve continuare anche nella schermata programmi , fgino all'avvio di un programma`
  - **Azione eseguita:** adattata la logica `ws2812_task` in `main/tasks.c` trattando anche lo stato `FSM_STATE_CREDIT` (schermata programmi) come stato con effetto dissolvenza attivo. La dissolvenza ora resta operativa in `IDLE` e `CREDIT` fino all'avvio del programma; la finestra di clear da 500ms viene applicata solo quando si esce da questi stati verso stati operativi. Aggiornato anche il resume dopo i lampeggi di fine ciclo per rispettare questa regola. Build di verifica eseguita con successo: `get_idf && idfc -b`.
- **Prompt utente:** `ora ll'avvio del programma tutti iled dinetano blu: il comportamento è 1. tutti spenti, 2. progressione verde/porpora`
  - **Azione eseguita:** corretto il ramo `RUNNING/PAUSED` in `main/tasks.c` per lasciare spenti (`RGB 0,0,0`) i LED non ancora coperti dalla progressione. In questo modo all'avvio programma la baseline è: 1) tutti spenti, 2) accensione progressiva solo verde/porpora in base allo stato `pre_fine_ciclo_active`. Build di verifica eseguita con successo: `get_idf && idfc -b`.
- **Prompt utente:** `dopo i 5 flash deve ripartire la dissolvenza sui led blu. I led blu non devono mai essere accesi fissi senza dissolvenza`
  - **Azione eseguita:** rimossa la modalità blu fisso nel `ws2812_task` (`main/tasks.c`): l'effetto dissolvenza blu è ora usato per tutti gli stati non `RUNNING/PAUSED` (incluso il post-lampeggio), eliminando il fallback con `led_fill_color` blu costante dopo i 5 flash. Verificata la ripartenza della dissolvenza subito dopo la sequenza di lampeggio e mantenuta la progressione verde/porpora solo durante esecuzione programma. Build di verifica eseguita con successo: `get_idf && idfc -b`.
- **Prompt utente:** `abbiamo degli errori sul ioexpander MH1001: ... ESP_ERR_NOT_FOUND ... clear bus failed ... ESP_ERR_INVALID_STATE ...`
  - **Azione eseguita:** analizzato il path `digital_io -> io_expander_init` e individuata fragilità nel recovery I2C: su errori recoverable (`ESP_ERR_INVALID_STATE/ESP_ERR_TIMEOUT/ESP_FAIL`) l'init usciva presto con `ESP_ERR_NOT_FOUND` senza recovery robusta del bus. Applicato fix in `components/io_expander/io_expander.c`: aggiunti helper `io_expander_is_recoverable_i2c_error` e `io_expander_recover_bus` (`i2c_master_bus_reset`), retry init aumentati a 3 tentativi, recovery esplicita su attach device e scrittura registri, e propagazione errore reale invece di mascherarlo sempre come `ESP_ERR_NOT_FOUND`. Build di verifica completata con successo: `get_idf && idfc -b`.
- **Prompt utente:** `il messaggio 'Confrma annullamento appare su emulator ma non su lvgl`
  - **Azione eseguita:** analizzato `components/lvgl_panel/lvgl_page_programmi.c` e individuato che il refresh periodico (`update_state`) sovrascriveva sempre il testo STOP con `program_stop`, annullando la conferma impostata da `on_stop_btn`. Corretto il ramo `running/paused` per rispettare `s_stop_confirm`: ora mostra `program_confirm_cancel` (`Conferma annullamento`) fino al secondo click, allineando il comportamento LVGL a quello emulator. Build di verifica completata con successo: `get_idf && idfc -b`.
- **Prompt utente:** `ricarica tasks.json dall'ultimo commit`
  - **Azione eseguita:** ripristinato `data/tasks.json` dall'ultimo commit (`HEAD`) tramite `git restore --source=HEAD --worktree -- data/tasks.json`, come richiesto.
- **Prompt utente:** `per la lingua italiana anche se modifico i nomi dei programma vengono rimessi a 'Programma 1)`
  - **Azione eseguita:** individuata doppia causa di overwrite dei nomi personalizzati. In `components/web_ui/web_ui_programs.c` il parser JSON ignorava il campo `name` e la serializzazione storage non lo salvava: ora `name` viene letto/salvato e i fallback i18n vengono applicati solo se `name` è vuoto. In `components/lvgl_panel/lvgl_page_programmi.c` la cache label privilegiava sempre `program_name_XX` i18n: ora privilegia `entry->name` (nome personalizzato) e usa i18n solo come fallback. Build di verifica completata con successo: `get_idf && idfc -b`.
- **Prompt utente:** `pagina /config/programs : i pulsanti linghe non caricano i dati per le altre lingue. questi dati sono in memoria assieme alle stringhe LVGL. Dopo averli caricati si può editarli e salvarli su i18n_v2.json`
  - **Azione eseguita:** corretto il parser frontend in `data/www/js/programs_04.js` (`parseProgramNameRecords`) per supportare entrambi i formati record restituiti da `/api/ui/texts`: campi `scope_id`/`scope` e chiave `key` sia testuale (`program_name_XX`) sia numerica legacy (`801..899` -> `program_id`). In questo modo i pulsanti lingua caricano correttamente i nomi programma anche per lingue diverse dall'italiano. Verificato anche il path di salvataggio già presente: il payload include `program_name_translations` e backend `api_programs_save` aggiorna `i18n_v2.json` tramite `device_config_update_program_text_i18n`. Tentato flash SPIFFS rapido (`get_idf && idfc -fs`): fallito per porta seriale occupata (`/dev/ttyACM0 busy`).
- **Prompt utente:** `in /config/programs ho le stringhe dei nomi programmi correte, in LVGL vedo sempre Programma 1 per tutte le lingue`
  - **Azione eseguita:** corretta la priorità risoluzione label in `components/lvgl_panel/lvgl_page_programmi.c` (`rebuild_program_label_cache_if_needed`): ripristinato lookup i18n (`program_name_XX`) come fonte primaria per rispettare la lingua runtime LVGL, usando `entry->name` solo come fallback quando la chiave i18n non è disponibile. Build di verifica completata con successo: `get_idf && idfc -b`.
- **Prompt utente:** `i led verdi della progressione devono avere massima luminosità 50%`
  - **Azione eseguita:** aggiornato `main/tasks.c` nel task `ws2812_task` riducendo l'intensità base del verde di progressione da `run_g = 170` a `run_g = 85` (50%), mantenendo invariata la logica di fade e la resa porpora in pre-fine ciclo. Build di verifica completata con successo: `get_idf && idfc -b`.
- **Prompt utente:** `su /config/programs ho le stringhe corrette; su LVGL ed Emulator vedo Programma 1 - correggere la fonte delle stringhe`
  - **Azione eseguita:** allineata la fonte nomi programma a i18n runtime su entrambi i path. In `components/web_ui/web_ui_programs.c` aggiunto fallback lookup legacy (`801..`) oltre a `program_name_XX` in `program_entry_refresh_name`, così l'Emulator riceve da `/api/programs` i nomi aggiornati da `i18n_v2.json` anche con cache/chiavi legacy. In `components/lvgl_panel/lvgl_panel.c` aggiornato `lvgl_panel_refresh_texts()` con `lvgl_i18n_reload()` prima del refresh, così LVGL ricarica le traduzioni correnti da `i18n_v2.json` invece di mantenere la cache vecchia. Build di verifica completata con successo: `get_idf && idfc -b`.
- **Prompt utente:** `quando viene selezionato un programma facciamo apparire un popup sopra l'area crediti con il nome del programma. Nella lista buttons con i programmi mettiamo il numero programma davanti al nome inscritto in un cerchio raggio 30px bianco con carattere nero`
  - **Azione eseguita:** implementata personalizzazione grafica pagina programmi in `components/lvgl_panel/lvgl_page_programmi.c`: aggiunto popup temporaneo (2.5s) sopra il box credito con nome programma selezionato, mostrato al click su pulsante programma (`on_prog_btn`). Restyling pulsanti programma con badge numerico circolare bianco (`60x60`, raggio 30) e numero nero, mantenendo il nome programma a destra del badge con ellissi su overflow. Build di verifica completata con successo: `get_idf && idfc -b`.
- **Prompt utente:** `il numero va a sx del nome per i pulsanti a larghezza intera e quelli della colonna a sx, mentre va dopo il nome nella colonna a dx. riporta l'altezza dei pulsanti a come prima dell'ultima modifica`
  - **Azione eseguita:** aggiornato `create_prog_button` in `components/lvgl_panel/lvgl_page_programmi.c` con layout condizionale per colonna: badge numerico a sinistra per pulsanti full-width e colonna sinistra; badge a destra per pulsanti colonna destra (nome prima del numero). Confermato mantenimento del calcolo altezze pulsanti preesistente (nessuna variazione nella logica `build_prog_buttons`). Build di verifica completata con successo: `get_idf && idfc -b`.
- **Prompt utente:** `sposta i 4 cerchi in alto a dx in modo che siano centrati con la bandiera`
  - **Azione eseguita:** aggiornato `components/lvgl_panel/lvgl_page_chrome.c` per allineare verticalmente i 4 cerchi al centro reale della bandiera: il posizionamento Y dei cerchi viene ora calcolato da `flag_top` e `flag_h` (`circles_top = flag_top + ((flag_h - circle_size) / 2)`) invece di usare offset fisso. Build di verifica completata con successo: `get_idf && idfc -b`.
- **Prompt utente:** `i pulsanti sono troppo grandi, ricalcolali per stare dentro la loro area: puoi anche ridurre di qualche px lo spazio verticale tra i pulsanti . allinea emulator alla nuova grafica della pagina programmi`
  - **Azione eseguita:** in `components/lvgl_panel/lvgl_page_programmi.c` ricalcolato il layout pulsanti su altezza reale del contenitore (`lv_obj_get_height(s_status_box)`), ridotti i gap verticali (`PROG_BTN_ROW_PAD`/`PROG_BTN_ROW_GAP` da 8/10 a 6/6) e aggiunto cap altezza pulsante (`PROG_BTN_MAX_H=98`) con centratura verticale del blocco pulsanti per garantire contenimento nell'area. Allineato emulator in `data/www/emulator.html` + `data/www/js/emulator_02.js`: nuova resa bottoni con badge numerico bianco + nome, inversione ordine in colonna destra, popup nome programma sopra area crediti, riduzione altezza area griglia e spacing verticale; aggiornato cache-busting script a `emulator_02.js?v=20260320d`. Build di verifica completata con successo: `get_idf && idfc -b`.
- **Prompt utente:** `allinea emultaor con la grafica attuale di programmi`
  - **Azione eseguita:** rifinito allineamento Emulator a layout LVGL corrente su `data/www/emulator.html` + `data/www/js/emulator_02.js`: griglia programmi ora usa altezza area completa e righe a quota fissa calcolata via JS (`MAX_BTN_H=98`, gap/padding verticali 6px), con centratura verticale del blocco pulsanti nel contenitore come in `build_prog_buttons`. Mantenuti badge numerici e ordine colonna destra invertito già introdotti; popup programma selezionato confermato sopra area crediti. Aggiornato cache-busting script a `emulator_02.js?v=20260320e`.
- **Prompt utente:** `il popup nome non appare`
  - **Azione eseguita:** corretto timing di visualizzazione popup sia su LVGL che su Emulator. In `components/lvgl_panel/lvgl_page_programmi.c` il popup viene ora mostrato immediatamente al click su programma valido/abilitato (prima della publish FSM), così non dipende dall'esito o dal timing dell'azione backend. In `data/www/js/emulator_02.js` il popup viene mostrato subito all'inizio del click handler, prima delle chiamate async start/pause. Build di verifica completata con successo: `get_idf && idfc -b`.
- **Prompt utente:** `il box crediti e l'orologio non è sincronizzato con lvgl. riduci il font del testo programmi`
  - **Azione eseguita:** allineato Emulator a LVGL su clock e credit box. In `data/www/js/emulator_02.js` l'orologio chrome ora mostra `HH:MM:SS` come LVGL; il box crediti è stato sincronizzato alla logica LVGL corrente forzando label `Crediti` e valore credito disponibile (non countdown secondi). In `data/www/emulator.html` ridotti i font per allineamento visivo (`.chrome-time` 35px, `.credit-value` 140px) e ridotto il font dei nomi programma (`.prog-btn-name` 30px).
- **Prompt utente:** `i cerchi in alto a dx escono dallo schermo, abbassali. In emulator il comportamento del tasto STOP non è sincronizzato con LVGL`
  - **Azione eseguita:** applicato offset verticale ai cerchi top-right nel chrome LVGL in `components/lvgl_panel/lvgl_page_chrome.c` (`circles_offset_y = 8`) per abbassarli rispetto alla bandiera. Allineato comportamento STOP Emulator in `data/www/js/emulator_02.js` alla logica LVGL: prima pressione imposta conferma e invia sempre `pause_toggle`, seconda pressione invia `program_stop`; aggiornato cache-busting frontend in `data/www/emulator.html` a `emulator_02.js?v=20260320f`. Build di verifica completata con successo: `get_idf && idfc -b`.
- **Prompt utente:** `il calcolo altezza pulsanti programma dinamico non è correto: sono passato da 110 a 8 programmi ma i tasti sono alti uguale`
  - **Azione eseguita:** corretto il calcolo dinamico rimuovendo il cap fisso introdotto in precedenza. In `components/lvgl_panel/lvgl_page_programmi.c` eliminato `PROG_BTN_MAX_H` e relativo clamp, così l'altezza torna davvero proporzionale al numero di righe (es. 8 programmi > 10 programmi). Allineato anche Emulator in `data/www/js/emulator_02.js` eliminando il limite `MAX_BTN_H` dal calcolo righe per mantenere parità visiva/di comportamento; aggiornato cache-busting a `data/www/emulator.html` con `emulator_02.js?v=20260320g`. Build di verifica completata con successo: `get_idf && idfc -b`.
- **Prompt utente:** `anche emulator deve seguire il numero dei programmi impostati. Le scritte del tasto stop non si sincronizzano tra lvgl e emulator`
  - **Azione eseguita:** aggiornato `data/www/js/emulator_02.js` per far seguire l'Emulator al numero programmi configurato (`n_prg`) letto da `/api/config`: introdotte `sanitizeProgramCount()` + `loadConfiguredProgramCount()` e filtraggio bottoni con `id > configuredProgramCount` prima del rendering metadata. Sincronizzate anche le scritte STOP con LVGL centralizzando i testi (`STOP` / `Conferma annullamento`) e riusandoli in tutti gli stati UI del pulsante. Aggiornato cache-busting in `data/www/emulator.html` a `emulator_02.js?v=20260320h`. Build di verifica completata con successo: `get_idf && idfc -b`.
- **Prompt utente:** `in /config metti i controlli della striscia LED in una sezione a se stante, po oltre al numero aggiungi i campi per settare i colori tramite un colorpicker per sezione verde della progressione, sezione porpora, colore standby (attualmente blu) , colore flash fine programma, numero flash fine programma . Crea anche una sezione Modbus dove  mettiamo un editbox per indicare il numero di expander connessi`
  - **Azione eseguita:** implementata nuova sezione dedicata `Striscia LED` su `/config` via `data/www/js/config_led_ui.js` (inclusa in `data/www/config.html`) con: enable LED, numero LED, color picker per progressione verde / porpora / standby / flash fine programma e campo numero flash fine programma. Agganciato caricamento da `/api/config` e salvataggio su `/api/config/save` con intercettore fetch. Rifinita sezione Modbus in `data/www/js/config_modbus_ui.js` con label esplicita `Numero expander connessi`. Esteso backend e persistenza config per nuovi campi LED: aggiornati `components/device_config/include/device_config.h`, `components/device_config/device_config.c` (default, load/save JSON), `components/web_ui/web_ui.c` (GET/POST `/api/config`), e `main/tasks.c` per usare runtime i colori configurati + conteggio flash fine programma. Build di verifica completata con successo: `get_idf && idfc -b`.
- **Prompt utente:** `il popup con il nome programma va spostato più in basso, con il bordo inveriore 10px sopra il bordo inferiore dell'area crediti`
  - **Azione eseguita:** aggiornato il posizionamento popup nome programma in `components/lvgl_panel/lvgl_page_programmi.c` dentro `build_status()`: sostituito l'anchor sopra al box crediti con posizionamento assoluto relativo a `credit_y`/`credit_h`, fissando il bordo inferiore del popup a `10px` sopra il bordo inferiore dell'area crediti (`y = credit_y + credit_h - popup_h - 10`). Build di verifica completata con successo: `get_idf && idfc -b`.
- **Prompt utente:** `Il popup con il nome programma appare sopra l'area crediti. va spostato più in basso DENTRO l'area crediti , con il bordo inveriore del popup 10px sopra il bordo inferiore dell'area crediti`
  - **Azione eseguita:** corretto `components/lvgl_panel/lvgl_page_programmi.c` facendo creare il popup come figlio diretto di `s_credit_box` e posizionandolo con `lv_obj_align(..., LV_ALIGN_BOTTOM_MID, 0, -10)`. In questo modo il popup resta sempre dentro l'area crediti con bordo inferiore a 10px dal bordo inferiore del box.
- **Prompt utente:** `RuntimeError: object name '/www/js/config_identity_layout.js' too long`
  - **Azione eseguita:** risolto limite SPIFFS `obj-name-len=32` rinominando `data/www/js/config_identity_layout.js` in `data/www/js/cfg_id_layout.js` e aggiornando l'include in `data/www/config.html`. Verifica compilazione completata con successo: `get_idf && idfc -b`.
- **Prompt utente:** `ho un conflitto in tasks.c : puoi correggerlo?`
  - **Azione eseguita:** risolto il conflitto logico in `main/tasks.c` dentro `ws2812_task` ripristinando la versione configurabile LED: rimossi valori hardcoded di colore/flash e uso dei campi `cfg->sensors.led_*` (run/prefine/standby/flash + `led_flash_count` con clamp 1..20). Aggiornata anche la logica blink fine ciclo con conteggio flash configurabile e colore flash configurabile.
- **Prompt utente:** `fai una copia con il codice attuale corretto di tasks.c e lvlg_page_programs.c`
  - **Azione eseguita:** create copie snapshot dei file correnti in `docs/snapshots/20260320_1812/`: `tasks.c` (da `main/tasks.c`) e `lvgl_page_programmi.c` (da `components/lvgl_panel/lvgl_page_programmi.c`). Posizionati in `docs/` per evitare impatto sulla build.
- **Prompt utente:** `allinea`
  - **Azione eseguita:** allineati i path locali in `dependencies.lock` sostituendo i riferimenti `/media/mauro/code/...` con `/home/mauro/1P/MicroHard/test_wave/...` per `waveshare/esp32_p4_nano` e `waveshare/esp_lcd_ili9881c`.
- **Prompt utente:** `ho spostato s_program_popup dentro s_credit_box, cambiala posizione per metterlo a 10 px dai bordi si s_credit_box in basso a dx`
  - **Azione eseguita:** aggiornato `components/lvgl_panel/lvgl_page_programmi.c` in `build_status()`: il popup è ora allineato dentro `s_credit_box` in basso a destra con margine `10px` da bordo destro e inferiore tramite `lv_obj_align(s_program_popup, LV_ALIGN_BOTTOM_RIGHT, -10, -10)`.
- **Prompt utente:** `il popup programma ora non appare`
  - **Azione eseguita:** fix in `components/lvgl_panel/lvgl_page_programmi.c` dentro `show_selected_program_popup()`: aggiunti `lv_obj_move_foreground(s_program_popup)` e `lv_obj_invalidate(s_program_popup)` subito dopo `lv_obj_clear_flag(...)` per garantire visibilità del popup sopra gli altri elementi nel `s_credit_box`.
- **Prompt utente:** `sposta config Striscia led sotto Display e Modbus sotoo Periferiche`
  - **Azione eseguita:** aggiornati gli anchor di inserimento in `data/www/js/config_led_ui.js` e `data/www/js/config_modbus_ui.js`: ora la sezione `Striscia LED` viene inserita subito dopo la sezione con titolo `Display`, mentre `Modbus` viene inserita subito dopo la sezione con titolo `Periferiche` (ricerca robusta su titoli normalizzati). Fallback invariato prima del blocco comandi se gli anchor non sono trovati.
- **Prompt utente:** `il messaggio non appare solo al primo programma selezionato, poi appare`
  - **Azione eseguita:** aggiornato `components/lvgl_panel/lvgl_page_programmi.c` in `on_prog_btn()`: la chiamata `show_selected_program_popup(pid)` è stata spostata subito dopo la lettura di `pid`, prima dei controlli `find_program_entry()`/`enabled`, così il popup appare già al primo click senza dipendere dal primo ciclo di validazione del programma.
- **Prompt utente:** `se annullo un programma deve continuare e completare il ciclo corrente ma non autoriavviarsi. Ora invece termina il ciclo corrente e poi si riavvia`
  - **Azione eseguita:** corretto il latch anti auto-riavvio in `components/lvgl_panel/lvgl_page_programmi.c`: rimosso il reset automatico di `s_stop_pressed` nel `panel_timer_cb()` all'uscita da `CREDIT`; il flag viene ora azzerato solo su nuova selezione manuale programma in `on_prog_btn()` (publish `tasks_publish_program_button_action` riuscito). In questo modo, dopo annullamento, il ciclo corrente può terminare ma non parte il riavvio automatico.
- **Prompt utente:** `al termine dell'esecuziione dei programmi dobbiamo ottenere un effetto come questa immagine`
  - **Azione eseguita:** implementato effetto fine programma in `components/lvgl_panel/lvgl_page_programmi.c`: overlay scuro full-screen + card bianca centrale con titolo, credito residuo e messaggio ringraziamento; trigger al passaggio `RUNNING/PAUSED -> CREDIT`, auto-hide dopo `3000ms` o su touch, e blocco temporaneo auto-restart mentre l'effetto è visibile. Aggiornate le traduzioni in `data/i18n_v2.json` con nuove chiavi `program_end_title`, `program_end_credits_fmt`, `program_end_message` in tutte le lingue.
- **Prompt utente:** `il messaggio deve rimanere visibile per un tempo definito in un parametro che rendiamo editalbile in /config/Identità Dispositivo (0 = non mostrare - 10sec.). la grafica sullo sfondo del messaggio deve essere sfuocata (come nella immagine)`
  - **Azione eseguita:** aggiunto parametro persistente `ui.program_end_message_sec` (0..10) in `components/device_config/include/device_config.h`, `components/device_config/device_config.c` (default/load/save) e `components/web_ui/web_ui.c` (GET/backup/save parse+serialize). Inserito controllo in `/config` sezione Identità con nuovo script `data/www/js/cfg_prog_end_sec.js` (field + clamp + hook save) e include in `data/www/config.html`. In `components/lvgl_panel/lvgl_page_programmi.c` la durata effetto è ora letta da config (`get_program_end_effect_ms()`), con `0` che disabilita il messaggio; sfondo aggiornato a overlay semi-trasparente con gradiente per resa blur-like.
- **Prompt utente:** `la app ha letto il codice qr e ha rilevato i 1000 cent (10 crediti) ma sul display rimane 0`
  - **Azione eseguita:** analizzato flusso mailbox/FSM e corretto `main/fsm.c` in `FSM_INPUT_EVENT_QR_CREDIT` e `FSM_INPUT_EVENT_CARD_CREDIT`: ora è consentito il rebind da `OPEN_PAYMENTS` a `VIRTUAL_LOCKED` quando lo stato è `CREDIT` e i bucket credito/used sono tutti a zero (caso tipico dopo touch activity prima dello scan QR). Mantiene il blocco "Pagamento aggiuntivo non consentito" nei casi reali di sessione già attiva con credito/uso presenti.
- **Prompt utente:** `adesso abbiamo un blocco sull'accettazione di VCR se ci sono gia ECR (monete) : rendiamo condizionale questo blocco mediante un define in cima afsm.c`
  - **Azione eseguita:** aggiunto define compile-time `FSM_BLOCK_VCD_WHEN_ECD_PRESENT` in testa a `main/fsm.c`. Il gating in `FSM_INPUT_EVENT_QR_CREDIT` e `FSM_INPUT_EVENT_CARD_CREDIT` ora è condizionale: con valore `1` mantiene il blocco attuale (richiede bucket/used a zero per rebind), con valore `0` consente rebind in `CREDIT` anche con sessione `OPEN_PAYMENTS` (mix VCD/ECD abilitato).
- **Prompt utente:** `controlla che le diciture riguardo ecd e vcd siano correte nei log. ... indica ECD VCD e euro`
  - **Azione eseguita:** aggiornati i log in `main/tasks.c` per rendere esplicite le voci credito: nel log periodico `Alive` e nel log di cambio stato ora vengono riportati `tot`, `ecd` e `vcd` con unità `euro` (es. `tot=8 euro ecd=3 euro vcd=5 euro`) al posto del precedente formato ambiguo `credit=%ldc`.
- **Prompt utente:** `il funzionamento di stop va corretto ...`
  - **Azione eseguita:** corretta la logica STOP/CONTINUA/CONFERMA tra `components/lvgl_panel/lvgl_page_programmi.c` e `main/fsm.c`:
    - pressione STOP #1: resta pausa + richiesta conferma;
    - pressione `Continua` sul pulsante programma: ripristina lo stato STOP normale (`s_stop_confirm=false`, `s_stop_pressed=false`) e riprende il ciclo;
    - pressione conferma annullamento: il ciclo corrente riprende (non stop immediato), viene impostato blocco auto-riavvio ciclo successivo (`s_stop_pressed=true`), in FSM vengono azzerati solo i bucket VCD (`vcd_coins/vcd_used/vcd_cents_residual`) mantenendo ECD valido; allo stop/fine ciclo la chiusura sessione virtuale ora azzera credito solo se rimane VCD effettivo.
- **Prompt utente:** `il credito VCD viene tenuti in memoria anche dopo lo scadere del timeout ... Se il blocco VCDafterECS è attivo dopo il primo ECS lo scanner va disattivato. se il blocco è inattivo lo scanner va attivato`
  - **Azione eseguita:**
    - in `main/fsm.c` (evento `FSM_EVENT_TIMEOUT` in stato `CREDIT`) aggiunto azzeramento esplicito bucket VCD (`vcd_coins`, `vcd_used`, `vcd_cents_residual`) prima della chiusura sessione, con riallineamento `credit_cents` per evitare che il VCD residuo rientri nella somma a successivi inserimenti ECD;
    - introdotta API `fsm_is_vcd_after_ecd_block_enabled()` (dichiarata in `main/fsm.h`, implementata in `main/fsm.c`) per esporre a runtime lo stato del blocco compile-time `FSM_BLOCK_VCD_WHEN_ECD_PRESENT`;
    - in `main/tasks.c` aggiunta policy scanner (`scanner_policy_should_force_off` + `scanner_policy_tick`): se blocco VCD-after-ECD attivo e rilevata attività ECD (`ecd_coins/ecd_used/ecd_cents_residual > 0`) lo scanner viene mantenuto OFF; se blocco non attivo lo scanner viene mantenuto ON (compatibile col cooldown QR già esistente).
- **Prompt utente:** `la forma e il colore dei pulsanti programma sono stati cambiato nell'ultimo build: rimettili come prina (verdi / spigoli rotondi). Al primo avvio la gettoniera risulta disabilitata`
  - **Azione eseguita:**
    - in `components/lvgl_panel/lvgl_page_programmi.c` ripristinata resa pulsanti programma verde/arrotondata: palette colori riportata su tonalità verdi (`COL_PROG`, `COL_PROG_ACT`, `COL_PROG_DIS`, `COL_PROG_PAUSED_BG`) e rimosso bordo spesso in stato attivo (`border_width` ora sempre `0`) che alterava la percezione degli spigoli;
    - in `main/tasks.c` resa robusta l'abilitazione iniziale gettoniera CCTALK: in `fsm_task` viene inviato una volta un `ACTION_ID_CCTALK_START` all'avvio quando CCTALK è abilitato e non si è in sessione VCD lockata (`cctalk_start_issued_at_boot`), evitando lo stato iniziale con gettoniera disabilitata.
- **Prompt utente:** `in/files appaiono molte stringhe non espanse es.: {{015}}`
  - **Azione eseguita:** analizzata la pipeline in `components/web_ui/webpages.c` (`send_html_localized_cached` + `localize_html_template`): in caso di allocazione fallita per la versione localizzata, la pagina andava in fallback raw e mostrava i placeholder `{{xxx}}`. Applicato fix in `psram_malloc`: se allocazione con `MALLOC_CAP_SPIRAM` fallisce, ora viene usato fallback su `malloc` heap standard, evitando il fallback raw su pagine grandi come `/files`.
- **Prompt utente:** `all'avvio la gettoniera è disabilitata`
  - **Azione eseguita:** in `main/tasks.c` (`fsm_task`) resa robusta l'abilitazione CCTALK all'avvio: rimosso il meccanismo one-shot e introdotto retry periodico (`CCTALK_START_RETRY_MS=2000`) finché `cctalk_driver_is_acceptor_enabled()` non risulta vero (in stato `CREDIT/ADS` e fuori sessione VCD lockata). In questo modo un primo `ACTION_ID_CCTALK_START` pubblicato ma non eseguito con successo non lascia più la gettoniera spenta al boot.

## Note
- In build restano warning preesistenti non bloccanti in file non legati direttamente alle modifiche funzionali odierne.
