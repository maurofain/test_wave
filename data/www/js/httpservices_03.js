window.callGetCustomers = async function(btn){ const orig = btn?btn.innerHTML:null; if(btn){ btn.disabled=true; btn.innerHTML='{{041}}'; } const reqEl=document.getElementById('hs_request'); const respEl=document.getElementById('hs_response'); try{ const codeEl=document.getElementById('customers_code'); const code=(codeEl&&codeEl.value)?codeEl.value.trim():'*'; const payload=JSON.stringify({ Code: (code||'*'), Telephone: '' }); const token=(localStorage.getItem('httpservices_token')||''); const headers={ 'Content-Type':'application/json', 'X-Request-Date': new Date().toUTCString() }; if(token) headers['Authorization']='Bearer '+token; if(reqEl){ let rq='{{042}}'+headers['X-Request-Date']; if(headers['Authorization']) rq += '{{043}}'+headers['Authorization']; rq += '{{044}}'+payload; reqEl.value=rq; } if(respEl){ respEl.value='{{045}}'; respEl.classList.add('hs-waiting'); } const res=await fetch('/api/getcustomers',{ method:'POST', headers:headers, body:payload, cache:'no-store' }); const txt=await res.text().catch(()=>('')); if(respEl){ respEl.classList.remove('hs-waiting'); respEl.value=txt || ('HTTP '+res.status); } return {ok:res.ok,status:res.status,text:txt}; }catch(e){ if(respEl){ respEl.classList.remove('hs-waiting'); respEl.value='Errore: '+e; } return {ok:false,status:0,text:String(e)}; } finally { if(btn){ btn.disabled=false; btn.innerHTML=orig; } } };window.hsKeepaliveClick = async function(btn){ const caller = document.activeElement; btn = btn || ((caller && caller.tagName==='BUTTON')?caller:null); const orig = btn?btn.innerHTML:null; const reqEl = document.getElementById('hs_request'); const resp = document.getElementById('hs_response'); if(btn){ btn.disabled=true; btn.innerHTML='{{046}}'; } try{ const payload = JSON.stringify({ status: 'OK', inputstates: '00010001', outputstates: '00000000', temperature: 2000, humidity: 4700, subdevices: [{ code: 'QRC', status: '{{047}}' }, { code: 'PRN1', status: 'OK' }] }); const tokenEl = document.getElementById('hs_token_value'); const token = (tokenEl && tokenEl.value) ? tokenEl.value : (localStorage.getItem('httpservices_token') || ''); const headers = { 'Content-Type':'application/json', 'X-Request-Date': new Date().toUTCString() }; if(token) headers['Authorization'] = 'Bearer ' + token; if(reqEl){ let rq = '{{048}}' + headers['X-Request-Date']; if(headers['Authorization']) rq += '{{043}}' + headers['Authorization']; rq += '{{044}}' + payload; reqEl.value = rq; } if(resp){ resp.value = '{{049}}'; resp.classList.add('hs-waiting'); } const res = await fetch('/api/keepalive', { method:'POST', headers: headers, body: payload, cache:'no-store' }); const txt = await res.text().catch(()=>('')); if(resp){ resp.classList.remove('hs-waiting'); resp.value = txt || ('HTTP ' + res.status); } }catch(e){ if(resp){ resp.classList.remove('hs-waiting'); resp.value = '{{050}}' + e; } } finally{ if(resp) resp.scrollTop = 0; if(btn){ btn.disabled=false; btn.innerHTML = orig; } } };window.callKeepalive = window.hsKeepaliveClick;window.addEventListener('DOMContentLoaded', async function(){ const stored = localStorage.getItem('httpservices_token'); try{ if(stored){ const respEl = document.getElementById('hs_response'); if(respEl) respEl.value = 'Token (localStorage):' + stored; try{ showToken(stored); }catch(e){} } const r = await fetch('/api/config'); if(r.ok){ const c = await r.json(); if(c.server){ const serial = c.server.ser || c.server.serial || ''; const password = c.server.pwd || c.server.password || ''; if(serial) document.getElementById('hs_serial').value = serial; if(password) document.getElementById('hs_password').value = password; } } }catch(e){} });async function submitLogin(){ const s = document.getElementById('hs_serial').value || ''; const p = document.getElementById('hs_password').value || ''; const btn = document.getElementById('hs_send'); const origBtn = btn ? btn.innerHTML : null; const reqEl = document.getElementById('hs_request'); const respEl = document.getElementById('hs_response'); if(respEl) { respEl.value = ''; respEl.classList.remove('hs-waiting'); } if(btn){ btn.disabled = true; btn.innerHTML = '{{046}}'; } if(!s || !p){ if(reqEl) reqEl.value = 'POST /api/login\n\n{}'; if(btn){ btn.disabled = false; btn.innerHTML = origBtn; } if(respEl) respEl.value = '{{052}}'; return; } try{ const body = JSON.stringify({ serial: s, password: p }); const headers = { 'Content-Type': 'application/json', 'X-Request-Date': new Date().toUTCString() }; if(reqEl) reqEl.value = '{{053}}' + headers['X-Request-Date'] + '{{044}}' + body; if(respEl){ respEl.value = ''; respEl.classList.add('hs-waiting'); } const res = await fetch('/api/login', { method: 'POST', headers: headers, body: body }); const txt = await res.text().catch(()=>('')); let j = null; try{ j = JSON.parse(txt); }catch(e){} if(res.ok){ if(respEl){ respEl.value = (j?JSON.stringify(j,null,2):txt); } if(j && j.access_token){ try{ showToken(j.access_token); }catch(e){}  localStorage.setItem('httpservices_token', j.access_token); if(respEl) respEl.value += '{{054}}' + j.access_token; } } else { if(respEl) respEl.value = txt || ('HTTP ' + res.status); } }catch(e){ console.error(e); const respEl2 = document.getElementById('hs_response'); if(respEl2) respEl2.value = '{{055}}' + e; } finally { if(respEl) respEl.classList.remove('hs-waiting'); if(btn){ btn.disabled = false; btn.innerHTML = origBtn; } } }// Token/JWT helper functions - showToken, copyToken, base64UrlDecodefunction base64UrlDecode(input){ try{ input = input.replace(/-/g,'+').replace(/_/g,'/'); while(input.length%4) input += '='; var decoded = atob(input); try{ return decodeURIComponent(Array.prototype.map.call(decoded, function(c){ return '%' + ('00' + c.charCodeAt(0).toString(16)).slice(-2); }).join('')); }catch(e){ return decoded; } }catch(e){ return null; } }function showToken(token){ try{ var v=document.getElementById('hs_token_value'); var isjwt=document.getElementById('hs_token_isjwt'); var hdr=document.getElementById('hs_jwt_header'); var pl=document.getElementById('hs_jwt_payload'); var sect=document.getElementById('hs_jwt_sections'); if(v){ if(typeof v.value !== 'undefined') v.value = token || ''; else v.textContent = token || '—'; } var parts = token?token.split('.') : []; var isJwt = (parts.length===3); if(isjwt) isjwt.textContent = isJwt? 'Yes' : 'No'; if(isJwt){ var h = base64UrlDecode(parts[0]); var p = base64UrlDecode(parts[1]); if(h && hdr){ try{ hdr.textContent = JSON.stringify(JSON.parse(h), null, 2); }catch(e){ hdr.textContent = h; } } if(p && pl){ try{ pl.textContent = JSON.stringify(JSON.parse(p), null, 2); }catch(e){ pl.textContent = p; } } if(sect) sect.style.display='block'; } else { if(hdr) hdr.textContent=''; if(pl) pl.textContent=''; if(sect) sect.style.display='none'; } }catch(e){} }function copyToken(){ try{ var el = document.getElementById('hs_token_value') || {value:'',textContent:''}; var t = (typeof el.value !== 'undefined' ? el.value : el.textContent) || ''; if(!t) return; if(navigator.clipboard && navigator.clipboard.writeText){ navigator.clipboard.writeText(t); alert('{{056}}'); } else { var ta=document.createElement('textarea'); ta.value=t; document.body.appendChild(ta); ta.select(); document.execCommand('copy'); ta.remove(); alert('{{056}}'); } }catch(e){} }// Generic POST helper that injects Authorization from Token textbox when availableasync function doPostLocal(path, body){ try{ const t = localStorage.getItem('httpservices_token') || ''; const headers = {'Content-Type':'application/json','X-Request-Date': new Date().toUTCString()}; if(t) headers['Authorization'] = 'Bearer ' + t; const reqEl = document.getElementById('hs_request'); const respEl = document.getElementById('hs_response'); const payload = body || '{}'; if(reqEl){ let rq = 'POST ' + path + '{{057}}' + headers['X-Request-Date']; if(headers['Authorization']) rq += '{{043}}' + headers['Authorization']; rq += '{{044}}' + payload; reqEl.value = rq; } if(respEl){ respEl.value = ''; respEl.classList.add('hs-waiting'); } const res = await fetch(path, { method: 'POST', headers: headers, body: payload }); const txt = await res.text().catch(()=>('')); if(respEl){ respEl.classList.remove('hs-waiting'); respEl.value = txt || ('HTTP ' + res.status); } return {ok:res.ok, status: res.status, text: txt}; }catch(e){ const respEl = document.getElementById('hs_response'); if(respEl){ respEl.classList.remove('hs-waiting'); respEl.value = 'Errore: ' + e; } return {ok:false, status:0, text: String(e)}; } }async function callGetConfig(){ const caller = document.activeElement; const btn = (caller && caller.tagName==='BUTTON')?caller:null; const orig = btn?btn.innerHTML:null; if(btn){ btn.disabled=true; btn.innerHTML='{{046}}'; } const resp = document.getElementById('hs_response'); try{ resp.value = '{{045}}'; const r = await doPostLocal('/api/getconfig'); resp.value = r.text || ('HTTP ' + r.status); }catch(e){ resp.value = 'Errore: ' + e; } finally{ resp.scrollTop = 0; if(btn){ btn.disabled=false; btn.innerHTML = orig; } } }async function callGetImages(){ const caller = document.activeElement; const btn = (caller && caller.tagName==='BUTTON')?caller:null; const orig = btn?btn.innerHTML:null; if(btn){ btn.disabled=true; btn.innerHTML='{{046}}'; } const resp = document.getElementById('hs_response'); try{ resp.value = '{{045}}'; const r = await doPostLocal('/api/getimages'); resp.value = r.text || ('HTTP ' + r.status); }catch(e){ resp.value = 'Errore: ' + e; } finally{ resp.scrollTop = 0; if(btn){ btn.disabled=false; btn.innerHTML = orig; } } }async function callGetTranslations(){ const caller = document.activeElement; const btn = (caller && caller.tagName==='BUTTON')?caller:null; const orig = btn?btn.innerHTML:null; if(btn){ btn.disabled=true; btn.innerHTML='{{046}}'; } const resp = document.getElementById('hs_response'); try{ resp.value = '{{045}}'; const r = await doPostLocal('/api/gettranslations'); resp.value = r.text || ('HTTP ' + r.status); }catch(e){ resp.value = 'Errore: ' + e; } finally{ resp.scrollTop = 0; if(btn){ btn.disabled=false; btn.innerHTML = orig; } } }async function callGetFirmware(){ const caller = document.activeElement; const btn = (caller && caller.tagName==='BUTTON')?caller:null; const orig = btn?btn.innerHTML:null; if(btn){ btn.disabled=true; btn.innerHTML='{{046}}'; } const resp = document.getElementById('hs_response'); try{ resp.value = '{{045}}'; const r = await doPostLocal('/api/getfirmware'); resp.value = r.text || ('HTTP ' + r.status); }catch(e){ resp.value = 'Errore: ' + e; } finally{ resp.scrollTop = 0; if(btn){ btn.disabled=false; btn.innerHTML = orig; } } }async function callPayment(){ const caller = document.activeElement; const btn = (caller && caller.tagName==='BUTTON')?caller:null; const orig = btn?btn.innerHTML:null; if(btn){ btn.disabled=true; btn.innerHTML='{{046}}'; } const resp = document.getElementById('hs_response'); try{ resp.value = '{{045}}'; const sample = JSON.stringify({ amount: 100 }); const r = await doPostLocal('/api/payment', sample); resp.value = r.text || ('HTTP ' + r.status); }catch(e){ resp.value = 'Errore: ' + e; } finally{ resp.scrollTop = 0; if(btn){ btn.disabled=false; btn.innerHTML = orig; } } }async function callPaymentOffline(){ const caller = document.activeElement; const btn = (caller && caller.tagName==='BUTTON')?caller:null; const orig = btn?btn.innerHTML:null; if(btn){ btn.disabled=true; btn.innerHTML='{{046}}'; } const resp = document.getElementById('hs_response'); try{ resp.value = '{{045}}'; const sample = JSON.stringify({ amount: 100 }); const r = await doPostLocal('/api/paymentoffline', sample); resp.value = r.text || ('HTTP ' + r.status); }catch(e){ resp.value = 'Errore: ' + e; } finally{ resp.scrollTop = 0; if(btn){ btn.disabled=false; btn.innerHTML = orig; } } }async function callServiceUsed(){ const caller = document.activeElement; const btn = (caller && caller.tagName==='BUTTON')?caller:null; const orig = btn?btn.innerHTML:null; if(btn){ btn.disabled=true; btn.innerHTML='{{046}}'; } const resp = document.getElementById('hs_response'); try{ resp.value = '{{045}}'; const sample = JSON.stringify({ service: 'test' }); const r = await doPostLocal('/api/serviceused', sample); resp.value = r.text || ('HTTP ' + r.status); }catch(e){ resp.value = 'Errore: ' + e; } finally{ resp.scrollTop = 0; if(btn){ btn.disabled=false; btn.innerHTML = orig; } } }window.callGetCustomers = async function(btn){ const orig = btn?btn.innerHTML:null; if(btn){ btn.disabled=true; btn.innerHTML='{{046}}'; } const resp = document.getElementById('hs_response'); const req = document.getElementById('hs_request'); try{ if(resp) resp.value = '{{045}}'; const codeEl = document.getElementById('customers_code'); const code = (codeEl && codeEl.value) ? codeEl.value.trim() : '*'; const payload = JSON.stringify({ Code: code || '*', Telephone: '' }); if(req) req.value = '{{058}}' + payload; const r = await doPostLocal('/api/getcustomers', payload); if(resp) resp.value = r.text || ('HTTP ' + r.status); }catch(e){ if(resp) resp.value = 'Errore: ' + e; else alert('Errore: ' + e); } finally{ if(btn){ btn.disabled=false; btn.innerHTML = orig; } } }async function callGetOperators(){ const caller = document.activeElement; const btn = (caller && caller.tagName==='BUTTON')?caller:null; const orig = btn?btn.innerHTML:null; if(btn){ btn.disabled=true; btn.innerHTML='{{046}}'; } const resp = document.getElementById('hs_response'); try{ resp.value = '{{045}}'; const r = await doPostLocal('/api/getoperators'); resp.value = r.text || ('HTTP ' + r.status); }catch(e){ resp.value = 'Errore: ' + e; } finally{ resp.scrollTop = 0; if(btn){ btn.disabled=false; btn.innerHTML = orig; } } }async function callActivity(){ const caller = document.activeElement; const btn = (caller && caller.tagName==='BUTTON')?caller:null; const orig = btn?btn.innerHTML:null; if(btn){ btn.disabled=true; btn.innerHTML='{{046}}'; } const resp = document.getElementById('hs_response'); try{ resp.value = '{{045}}'; const r = await doPostLocal('/api/activity', JSON.stringify({ action: 'test' })); resp.value = r.text || ('HTTP ' + r.status); }catch(e){ resp.value = 'Errore: ' + e; } finally{ resp.scrollTop = 0; if(btn){ btn.disabled=false; btn.innerHTML = orig; } } }function clearResponse(){ try{ document.getElementById('hs_response').value = ''; }catch(e){} }
